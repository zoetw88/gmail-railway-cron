[project]
name = "gmail-railway-cron"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["google-api-python-client==2.176.0", "google-auth==2.40.3"]

[project.optional-dependencies]
test = ["pytest==8.4.1"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

